name: Branark Closing Ledger Process Test

on:
  workflow_dispatch:
    inputs:
      test_file_name:
        description: "Drive에 생성할 테스트 파일명"
        required: true
        default: "branark-closing-ledger-test.txt"
      order_date:
        description: "일일 마감 양식 주문일"
        required: true
        default: "2026-07-03"
      product_name:
        description: "테스트 상품명"
        required: true
        default: "[TEST] 김밥집 궁채 2kg"
      quantity:
        description: "테스트 수량"
        required: true
        default: "1"
      supply_price:
        description: "테스트 공급단가"
        required: true
        default: "7425"
      allow_duplicate_file:
        description: "같은 파일명이 있어도 생성 허용"
        required: true
        default: "false"
        type: choice
        options:
          - "false"
          - "true"

permissions:
  contents: read

jobs:
  process-test:
    name: Call Apps Script closing ledger process
    runs-on: ubuntu-latest
    env:
      APPS_SCRIPT_WEB_APP_URL: ${{ secrets.APPS_SCRIPT_WEB_APP_URL || vars.APPS_SCRIPT_WEB_APP_URL }}
      APPS_SCRIPT_API_TOKEN: ${{ secrets.APPS_SCRIPT_API_TOKEN || vars.APPS_SCRIPT_API_TOKEN }}
      TEST_FILE_NAME: ${{ inputs.test_file_name }}
      ORDER_DATE: ${{ inputs.order_date }}
      PRODUCT_NAME: ${{ inputs.product_name }}
      QUANTITY: ${{ inputs.quantity }}
      SUPPLY_PRICE: ${{ inputs.supply_price }}
      ALLOW_DUPLICATE_FILE: ${{ inputs.allow_duplicate_file }}
    steps:
      - name: Call Google Apps Script Web App
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import json
          import os
          import sys
          import urllib.error
          import urllib.request

          required = ["APPS_SCRIPT_WEB_APP_URL", "APPS_SCRIPT_API_TOKEN"]
          missing = [name for name in required if not os.environ.get(name)]
          if missing:
              raise SystemExit("필수 설정값 누락: " + ", ".join(missing))

          url = os.environ["APPS_SCRIPT_WEB_APP_URL"].strip()
          if not url.startswith("https://script.google.com/"):
              raise SystemExit("APPS_SCRIPT_WEB_APP_URL은 Google Apps Script Web App 배포 URL이어야 합니다.")

          payload = {
              "apiToken": os.environ["APPS_SCRIPT_API_TOKEN"],
              "testFileName": os.environ["TEST_FILE_NAME"].strip(),
              "orderDate": os.environ["ORDER_DATE"].strip(),
              "productName": os.environ["PRODUCT_NAME"].strip(),
              "quantity": int(os.environ["QUANTITY"]),
              "supplyPrice": int(os.environ["SUPPLY_PRICE"]),
              "allowDuplicateFile": os.environ["ALLOW_DUPLICATE_FILE"].lower() == "true",
          }

          body = json.dumps(payload, ensure_ascii=False).encode("utf-8")
          request = urllib.request.Request(
              url,
              data=body,
              headers={"Content-Type": "application/json"},
              method="POST",
          )

          try:
              with urllib.request.urlopen(request, timeout=90) as response:
                  response_body = response.read().decode("utf-8")
          except urllib.error.HTTPError as error:
              error_body = error.read().decode("utf-8", errors="replace")
              print(error_body)
              raise SystemExit(f"Apps Script 호출 실패: HTTP {error.code}")
          except urllib.error.URLError as error:
              raise SystemExit(f"Apps Script 연결 실패: {error}")

          print(response_body)

          try:
              result = json.loads(response_body)
          except json.JSONDecodeError as error:
              raise SystemExit(f"Apps Script 응답이 JSON이 아닙니다: {error}")

          if not result.get("ok"):
              raise SystemExit("Apps Script 처리 실패: " + str(result.get("error")))

          print("브랜아크 일일마감 Apps Script 처리 완료")
          print("- Drive file:", result.get("fileName"), result.get("fileId"))
          print("- Drive URL:", result.get("fileUrl"))
          print("- Daily sheet:", result.get("spreadsheetTitle"), "/", result.get("sheetName"))
          print("- Appended row:", result.get("appendedRow"))
          PY
